apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: root-lamp-clone-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: iscsi-zfs
  dataSource:
    kind: PersistentVolumeClaim
    name: root-lamp-0